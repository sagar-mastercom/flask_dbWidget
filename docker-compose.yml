version: '3'
services:
  flask-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '8000:80'
      - '8082:8000'
      - '5000:443'
    volumes:
      - './certs:/certs'